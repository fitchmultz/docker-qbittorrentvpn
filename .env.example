# Example environment for docker-qbittorrentvpn
# Build requirements: none (image builds without .env)
# Run requirements: set `LAN_NETWORK`, `CONFIG_PATH`, `DOWNLOADS_PATH` (compose volumes),
#   and supply a VPN config file: /config/openvpn/*.ovpn or /config/wireguard/wg0.conf

# --- Host paths (used by docker-compose) ---
CONFIG_PATH=./config            # required (run/compose)
DOWNLOADS_PATH=./downloads      # required (run/compose)
DOWNLOADS_MOVIES_PATH=./downloads/movies
DOWNLOADS_TV_PATH=./downloads/tv
DOWNLOADS_MUSIC_PATH=./downloads/music
DOWNLOADS_TORRENT_FILES=./downloads/torrentfiles

# --- VPN and networking ---
VPN_ENABLED=yes                 # optional (defaults to yes)
VPN_TYPE=openvpn                # optional (openvpn|wireguard)
VPN_USERNAME=
VPN_PASSWORD=
LEGACY_IPTABLES=no              # optional
LAN_NETWORK=192.168.1.0/24      # required (run)
NAME_SERVERS=1.1.1.1,8.8.8.8,1.0.0.1,8.8.4.4
ADDITIONAL_PORTS=
VPN_OPTIONS=
DEBUG=false

# --- qBittorrent / runtime ---
PUID=1000                       # safest: non-root UID (typical host user)
PGID=1000                       # safest: non-root GID (typical host group)
UMASK=002
ENABLE_SSL=yes                  # optional (self-signed cert generated)
HOST_OS=

# --- Health check behavior ---
HEALTH_CHECK_HOST=one.one.one.one
HEALTH_CHECK_INTERVAL=300
HEALTH_CHECK_SILENT=1
HEALTH_CHECK_AMOUNT=1
RESTART_CONTAINER=yes

# --- Optional extras ---
INSTALL_PYTHON3=no
